import template from './placeOrder.html';
import controller from './placeOrder.controller';

let placeOrderComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default placeOrderComponent;
