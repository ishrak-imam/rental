import template from './lighting.html';
import controller from './lighting.controller';

let lightingComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default lightingComponent;
