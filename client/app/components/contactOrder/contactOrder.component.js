import template from './contactOrder.html';
import controller from './contactOrder.controller';

let contactOrderComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default contactOrderComponent;
