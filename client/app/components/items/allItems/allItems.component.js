import template from './allItems.html';
import controller from './allItems.controller';

let allItemsComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default allItemsComponent;
