import template from './mobileDevice.html';
import controller from './mobileDevice.controller';

let mobileDeviceComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default mobileDeviceComponent;
